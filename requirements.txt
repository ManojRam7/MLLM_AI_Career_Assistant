# Core (free to run)
requests>=2.31
python-dateutil>=2.9
rapidfuzz>=3.6
psycopg[binary]>=3.1        # Supabase / Postgres
PyYAML>=6.0
python-dotenv>=1.0
python-docx>=1.1            # CV / cover-letter .docx
openpyxl>=3.1              # read the company bucket-list .xlsx

# LLMs
google-genai>=0.3           # Gemini 2.5 Pro (current SDK)
# OpenAI (GPT-5.6) and DeepSeek (v4) are called over their REST APIs via `requests` — no SDK needed.
# Groq retired 2026-08.

# Dashboard (also what Streamlit Community Cloud installs from this file)
streamlit>=1.36
pandas>=2.0
altair>=5.0                # coloured, value-labelled charts (bundled with streamlit; pinned for safety)

# Optional extras (only if you enable them)
# playwright>=1.44          # JS career-page fallback  (run: playwright install chromium)
